import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1
    },
    flatListView: {
        flex: 1
    },
    selectedAlbums: {
        flex: 2
    },
    selectedSongs: {
        flex: 1
    }
});

export default styles;
